<h1>LineaPlan</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Preciounitario</th>
            <td>{{ entity.preciounitario }}</td>
        </tr>
        <tr>
            <th>Cantidadpedido</th>
            <td>{{ entity.cantidadPedido }}</td>
        </tr>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('lineaplan') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('lineaplan_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('lineaplan_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
